# Seed saves are not required

Load any in-world save, then `cmp_join`. A new guest lands at the host. A returning player uses their last pose if that worldspace matches the host.

The dedicated server does not run Fallout 4 and cannot merge `.fos` files.

Keep Bethesda binaries out of this folder. `.fos` files stay out of git.
